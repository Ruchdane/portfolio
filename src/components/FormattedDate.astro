---
interface Props {
	date: Date;
	format?: Intl.DateTimeFormatOptions,
}

const { date, format } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('fr-FR', format ?? {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
