---
import i18n from "i18next";

export interface Props {
  locale: string;
}

const { locale } = Astro.props;
const currentLocale = i18n.language || "fr";
---

{currentLocale === locale && <slot />}
